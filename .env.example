# Copy to .env (gitignored) for local development.
# OpenRouter keys start with sk-or- and are auto-routed even if you use OPENAI_API_KEY.

OPENROUTER_API_KEY=
# OPENAI_MODEL=openai/gpt-4o-mini

# Optional local PayMongo (leave blank to use Unlock Pro stub)
# PAYMONGO_SECRET_KEY=sk_test_...

# Object storage (Cloudflare R2 or AWS S3). Same keys locally and on Render.
# Development uses R2 when these are set so uploads match production.
# ACTIVE_STORAGE_SERVICE=cloud
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_BUCKET=
# AWS_REGION=auto
# AWS_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# AWS_FORCE_PATH_STYLE=true

# Poster formats: all = 6 in two batches of 3; core = squares only (emergency low-RAM)
# POSTER_FORMAT_SET=all

# Transactional email (Resend SMTP example)
# SMTP_ADDRESS=smtp.resend.com
# SMTP_PORT=587
# SMTP_USERNAME=resend
# SMTP_PASSWORD=re_...
# MAILER_FROM=ListingPack <noreply@yourdomain.com>
# APP_HOST=localhost:3000
